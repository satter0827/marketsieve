# marketsieve-ai

`marketsieve-ai` prepares grounded report requests, validates manually saved AI responses, and
renders explanations from MarketSieve-owned facts. It depends only on the public `marketsieve` SDK
and contains no HTTP client, browser automation, CLI, configuration, or persistence code.

The existing `marketsieve-agent` package remains available for direct model-provider integrations.
