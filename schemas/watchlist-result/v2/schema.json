{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:marketsieve:schema:watchlist-result:2.0.0",
  "title": "MarketSieve watchlist result",
  "type": "object",
  "required": ["watchlist_id", "schema", "as_of", "previous_watchlist_id", "change", "items"],
  "properties": {
    "watchlist_id": {"type": ["string", "null"], "pattern": "^[0-9a-f]{64}$"},
    "schema": {"const": "watchlist-result/v2"},
    "as_of": {"type": ["string", "null"], "format": "date-time"},
    "previous_watchlist_id": {"type": ["string", "null"], "pattern": "^[0-9a-f]{64}$"},
    "change": {
      "type": ["object", "null"],
      "required": ["operation", "instrument"],
      "properties": {
        "operation": {"enum": ["add", "remove"]},
        "instrument": {"type": "string", "pattern": "^(BATS|XTKS|XNAS|XNYS):.+$"}
      },
      "additionalProperties": false
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["key", "instrument"],
        "properties": {
          "key": {"type": "string", "pattern": "^(BATS|XTKS|XNAS|XNYS):.+$"},
          "instrument": {"$ref": "#/$defs/instrument"}
        },
        "additionalProperties": false
      }
    },
    "history_ids": {
      "type": "array",
      "items": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
    }
  },
  "additionalProperties": false,
  "$defs": {
    "instrument": {
      "type": "object",
      "required": ["mic", "symbol", "currency", "timezone", "type"],
      "properties": {
        "mic": {"enum": ["BATS", "XTKS", "XNAS", "XNYS"]},
        "symbol": {"type": "string", "minLength": 1},
        "currency": {"enum": ["JPY", "USD"]},
        "timezone": {"enum": ["Asia/Tokyo", "America/New_York"]},
        "type": {"const": "equity"}
      },
      "additionalProperties": false
    }
  }
}
